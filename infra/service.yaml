apiVersion: v1
kind: Service
metadata:
  name: devops-lab-service
spec:
  type: NodePort
  selector:
    app: devops-lab-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080

